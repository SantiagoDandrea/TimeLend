{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @timelend/database build && pnpm build",
  "crons": [
    {
      "path": "/api/automation/finalize-expired-failures",
      "schedule": "0 3 * * *"
    }
  ],
  "functions": {
    "src/app.ts": {
      "maxDuration": 300
    }
  }
}
